// Create a type "Gender" using a union type that can contain values "male" and "female".
// Declare a variable myGender of this type.

type Gender = "male" | "female";

const myGender: Gender = "female";

export { myGender };
